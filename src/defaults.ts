import { Options, Proverb, View } from "./types";

const DEFAULTS: Options = {
    view: View.Item,
    id: null,
    lang: "en",
    pageSize: 120,
    random: true
};

const COLORS = {
    iconFill: "#333"
};

export default DEFAULTS;
