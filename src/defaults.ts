import { Options, View } from "./types/types";

const DEFAULTS: Options = {
    view: View.Item,
    id: 0,
    lang: "en",
    pageSize: 120,
    random: true
};

const COLORS = {
    iconFill: "#333"
};

export { DEFAULTS, COLORS };
